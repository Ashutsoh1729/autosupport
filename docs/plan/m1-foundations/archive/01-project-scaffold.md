# M1 Foundations — Plan 1: Project Scaffold

Status: Completed

## Description
Scaffold the Next.js + TypeScript application (App Router, Tailwind, src dir) with a minimal branded landing page, and get it deployed and live on Vercel.

## Goals
- Working Next.js dev server with TypeScript and Tailwind configured
- Clean base project structure (`src/`, `src/app/`)
- `.env.example` with placeholders for `DATABASE_URL` and `BETTER_AUTH_SECRET`
- Initial git commit, then a live deploy on Vercel

## Workflow
`create-next-app` generates the app → verify dev server and build → replace boilerplate with a minimal landing page → create `.env.example` → initial commit → deploy to Vercel → confirm the app loads at the Vercel URL.

## Implementation Steps

### Step 1: Scaffold the app
- [x] From the project root, run: `npx create-next-app@latest . --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --yes`
- [x] If create-next-app refuses because of existing files, temporarily move the conflicting files out of the root, scaffold, then restore them
- [x] Start the dev server (`npm run dev`) and confirm the default page renders at http://localhost:3000
- [x] Run `npm run build` and confirm it completes without errors

### Step 2: Landing page + env example
- [x] Replace the generated boilerplate in `src/app/page.tsx` with a minimal branded landing page (product name, tagline, "Get started" button linking to `/login`)
- [x] Create `.env.example` with placeholders:
  - `DATABASE_URL=postgresql://...`
  - `BETTER_AUTH_SECRET=...`
- [x] Run `npm run build` again to confirm the changes compile

### Step 3: Initial commit
- [x] `git add .` and commit with message `chore: scaffold Next.js + TypeScript app`

### Step 4: Deploy to Vercel
- [x] Push the repo to GitHub and import it into Vercel (or run `npx vercel --prod` after logging in)
- [x] In Vercel project settings, add `DATABASE_URL` and `BETTER_AUTH_SECRET` env vars (placeholders are fine for now)
- [x] Confirm the app loads at the production Vercel URL

### Step 5: Create GitHub remote & push
- [x] Create a private GitHub repo with `gh repo create autosupport --private --source=. --remote=origin --push`
- [x] Confirm the local `main` branch is pushed and tracked (fetch/pull works)

---

## Files to Modify
- `src/app/page.tsx` — replace boilerplate with the landing page
- `.env.example` — new, env placeholders
- Generated by create-next-app: `package.json`, `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `tailwind.config.ts`, `src/app/layout.tsx`, `src/app/globals.css`

## Functional Components

| Function | File | Role |
|----------|------|------|
| `page` | `src/app/page.tsx` | Renders the landing page |

## Data Model
None in this plan.

## Boundaries
- No external systems yet. Vercel deploy requires a Vercel account / GitHub connection (manual, user-assisted).

## Considerations
- Use the non-interactive flags above so create-next-app does not pause for prompts.
- `.git` and `docs/` already exist in the root; only conflicting files (e.g. `package.json`, `app/`) cause create-next-app to abort — the plan handles that case.
- `.env*` is already gitignored; only `.env.example` is committed.
- `DATABASE_URL` and `BETTER_AUTH_SECRET` are placeholders until plans 2 and 3.

---

## Execution Notes

- **Architecture** — Next.js 16.3.0 (App Router, Turbopack) scaffolded via `create-next-app` with TypeScript, Tailwind v4, ESLint, and `src/` directory. Root layout uses the generated Geist fonts; landing page is a server component rendering a minimal branded hero. `@/*` import alias configured. `.gitignore` was replaced by the scaffolder — restored the `docs/notes/whiteboard.md` ignore and added `!.env.example` so the template is committed.
- **How each step was implemented**
  - Step 1: Ran `npx create-next-app@latest . --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --yes`. Verified via `curl` on the dev server (200) and `npm run build` (passes). Note: Next.js is v16, not v15 — per AGENTS.md, docs in `node_modules/next/dist/docs/` were consulted (layouts-and-pages); conventions matched the App Router model.
  - Step 2: Replaced boilerplate `src/app/page.tsx` with a branded landing page (product name "AutoSupport", tagline, CTA links to `/login` and `/signup` — those routes 404 until plan 3). Updated metadata in `src/app/layout.tsx`. Created `.env.example` with `DATABASE_URL` and `BETTER_AUTH_SECRET` placeholders. Re-ran `npm run build` and `npm run lint` (both pass).
  - Step 3: Committed `chore: scaffold Next.js + TypeScript app` (commit `465f3db`).
  - Step 4: Linked to Vercel project `autosupport` (scope `ashutsoh1729s-projects`) and deployed via CLI. The installed CLI (46.0.5) was rejected by Vercel (requires ≥47.2.2), so it was upgraded to 58.5.1 and redeployed. Production URL `https://autosupport-peach.vercel.app` returns 200. Added placeholder `DATABASE_URL` and `BETTER_AUTH_SECRET` env vars to the Production environment (Sensitive type).
  - Step 5: Created private repo `https://github.com/Ashutsoh1729/autosupport` via `gh repo create`. The initial push failed because git used the SSH protocol (`git@ssh.github.com: Permission denied (publickey)` — no SSH key on the account). Fixed by running `gh auth setup-git` and switching the origin URL to HTTPS (`https://github.com/Ashutsoh1729/autosupport.git`), then `git push -u origin main` succeeded. Branch `main` now tracks `origin/main`.
- **Files Created** — `src/app/page.tsx` (landing page), `.env.example`, plus all scaffolded files (package.json, tsconfig.json, next.config.ts, eslint.config.mjs, postcss.config.mjs, tailwind via globals.css, src/app/layout.tsx, src/app/globals.css, public/*, README.md, AGENTS.md, CLAUDE.md).
- **Files Modified** — `.gitignore` (re-added whiteboard ignore + `!.env.example`); `src/app/layout.tsx` (metadata).
- **Remaining / Known Gaps**
  - `/login` and `/signup` links on the landing page 404 until plan 3 (expected).
  - GitHub integration with Vercel not wired yet — the repo now exists at `github.com/Ashutsoh1729/autosupport`; enabling auto-deploy requires granting the `autosupport` repo access to Vercel's GitHub app.
  - Env vars are placeholders until plans 2/3 provide real values (Neon connection string, generated auth secret).
